version: "3.8"

services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_DB: celcoin
      POSTGRES_USER: celcoin
      POSTGRES_PASSWORD: root
    ports:
      - "5432:5432"